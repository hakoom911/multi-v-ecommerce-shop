<!DOCTYPE html>
<html >
<head>
    <title>Your Hshop OTP Code</title>
  <meta http-equiv="Content-Type"content="text/html; charset=UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style type="text/css">
    :root {
      --bg: #f5f7fa;
      --card-bg: #ffffff;
      --brand: #0070f3;
      --text-primary: #1f2937;
      --text-secondary: #4b5563;
      --radius: 8px;
      --shadow: 0 4px 12px rgba(0, 0, 0, 0.05);
      --font: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
    }
    * { box-sizing: border-box; margin:0; padding:0; }
    body {
      background: var(--bg);
      font-family: var(--font);
      color: var(--text-primary);
      line-height: 1.5;
      padding: 20px;
    }
    .container {
      max-width: 600px;
      margin: auto;
      background: var(--card-bg);
      border-radius: var(--radius);
      overflow: hidden;
      box-shadow: var(--shadow);
    }
    .header {
      background: var(--brand);
      text-align: center;
      padding: 24px;
    }
    .header h1 {
      color: #fff;
      font-size: 22px;
      font-weight: 600;
    }
    .content {
      padding: 32px 24px;
    }
    .greeting {
      margin-bottom: 16px;
      font-size: 16px;
      color: var(--text-secondary);
    }
    .otp {
      display: block;
      text-align: center;
      font-size: 36px;
      font-weight: 700;
      letter-spacing: 6px;
      margin: 24px 0;
      color: var(--brand);
    }
    .instructions {
      font-size: 14px;
      color: var(--text-secondary);
      margin-bottom: 24px;
    }
    .footer {
      background: #f0f4f8;
      padding: 20px 24px;
      font-size: 12px;
      color: var(--text-secondary);
      text-align: center;
    }
    .footer a {
      color: var(--brand);
      text-decoration: none;
    }
    @media (max-width: 480px) {
      .content { padding: 24px 16px; }
      .otp { font-size: 28px; }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Hshop One‑Time Passcode</h1>
    </div>
    <div class="content">
      <p class="greeting">Hi <%= name %>,</p>
      <p class="instructions">
       Thank you for registering with Hshop. To activate your account please Use the activation code below. It expires in 5 minutes.
      </p>
      <span class="otp"><%= otp %></span>
      <p class="instructions">
        If you did not request this code, simply ignore this email no further action is needed.
      </p>
    </div>
    <div class="footer">
      <p>Need help? Contact us at <a href="mailto:support@hshop.com">support@hshop.com</a>.</p>
      <p>&copy; <%= new Date().getFullYear() %> Hshop Inc.</p>
    </div>
  </div>
</body>
</html>
